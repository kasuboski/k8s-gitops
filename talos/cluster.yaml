nodes:
  blueberry:
    role: controlplane
    patches:
    - austin-labels-patch.yaml
    - ephemeral-usb-patch.yaml
    - sdcard-install-patch.yaml
    - etcd-advertise-patch.yaml
    - vip-patch.yaml
  cherry:
    role: controlplane
    patches:
    - austin-labels-patch.yaml
    - ephemeral-usb-patch.yaml
    - sdcard-install-patch.yaml
    - etcd-advertise-patch.yaml
    - vip-patch.yaml
  pumpkin:
    role: controlplane
    patches:
    - austin-labels-patch.yaml
    - ephemeral-usb-patch.yaml
    - sdcard-install-patch.yaml
    - etcd-advertise-patch.yaml
    - vip-patch.yaml
  apple:
    role: worker
    patches:
    - austin-labels-patch.yaml
    - sdcard-install-patch.yaml
  adel:
    role: worker
    patches:
    - austin-labels-patch.yaml