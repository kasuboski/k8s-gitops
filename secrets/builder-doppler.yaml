apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: builder
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: builder-token
  managedSecret:
    name: builder
    namespace: builder
